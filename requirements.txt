# A book site = this thin project + the parody-web package. Pin it; bump to roll
# out renderer updates (automatable via Dependabot/Renovate).
parody-web>=0.10.2,<0.11
gunicorn>=22.0
uvicorn[standard]>=0.30
whitenoise>=6.6
dj-database-url>=2.2
psycopg[binary]>=3.2
